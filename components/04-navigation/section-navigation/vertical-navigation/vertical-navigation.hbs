
<nav class="{{nav-wrapper-classes}}" id="section-nav" aria-labelledby="menu-btn" role="menu">
  <ul class="{{nav-inner-classes}}">
    {{#each nav-items}}
      <li class="nav__item pluto {{item_class}}">
        <a class="nav__link" href="{{item_url}}" role="menuitem">{{item}}</a>
      </li>
    {{/each}}
  </ul>
</nav>
