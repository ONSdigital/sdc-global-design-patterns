<label class="label{{#if label_inline}} label--inline{{/if}} u-fs-r--b {{label_class}}"
  {{#if label_for}}for="{{label_for}}"{{/if}}
  {{#if label_id}}id="{{label_id}}"{{/if}}>{{label_text}}{{#if label_description}}
  <br/><span class="label__description u-fs-s">{{label_description}}</span>
  {{/if}}</label>
